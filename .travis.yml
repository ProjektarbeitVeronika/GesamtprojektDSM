language: java

jdk:
  - oraclejdk8
  


addons:
  apt:
    packages:
      - language-pack-en
      - language-pack-de
      - oracle-java8-installer

script:
- script:
- export CLASSPATH=$PWD/lib/:$CLASSPATH
- cd src/jUnitTests
- javac *.java
- cd ../algorithm
- javac Measurement.java


#lesezeichenexport CLASSPATH=.:lib/*  export CLASSPATH=$CLASSPATH:$PWD/lib:$PWD/src
# home: /home/travis